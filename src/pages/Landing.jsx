import { Box, Text } from "@chakra-ui/react";
import React from "react";

const Landing = (props) => {
  return (
    <Box>
      <Text align={"center"}>Homepage</Text>
    </Box>
  );
};

export default Landing;
